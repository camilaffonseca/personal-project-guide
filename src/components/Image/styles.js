import styled from 'styled-components'

import Image from './Image'

const MarginBottomImage = styled(Image)`
  margin-bottom: 7rem;
`

const MarginBottomTopImage = styled(Image)`
  margin: 1rem 0 4rem 0;
`

export { MarginBottomImage, MarginBottomTopImage }
